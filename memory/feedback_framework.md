---
name: framework-preference
description: User wants React.js + Node.js frameworks, not plain HTML files
type: feedback
---

Use React.js with Next.js (App Router) as the default framework for this project. Node.js is expected for any backend logic (Next.js API routes satisfy this).

**Why:** User explicitly corrected plain HTML approach — expects proper React/Node.js framework structure.

**How to apply:** Always scaffold Next.js App Router projects with TypeScript and Tailwind CSS. Never deliver a standalone HTML file as the primary output.
